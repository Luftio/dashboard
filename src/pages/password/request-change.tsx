import React from "react";
import Default from "../../components/layouts/Default";
import RequestChange from "../../components/templates/RequestChange";

const RequestChangePage: React.FC = () => {
  return (
    <Default>
      <RequestChange />
    </Default>
  );
};

export default RequestChangePage;
