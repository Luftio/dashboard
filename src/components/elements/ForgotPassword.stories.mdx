import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import ForgotPassword from "./ForgotPassword";

<Meta title="Elements/Forgot password" component={ForgotPassword} />

# Forgot Password

This is a simple text that is displayed under the input of type password on the sign in page. It takes you to the request change page.

export const Component = (args) => <ForgotPassword>{args.text}</ForgotPassword>;

<Canvas>
  <Story
    name="Forgot password"
    args={{
      text: "Forgot password",
    }}>
    {Component.bind({})}
  </Story>
</Canvas>
