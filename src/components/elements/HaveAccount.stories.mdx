import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import HaveAccount from "./HaveAccount";

<Meta title="Elements/Have account" component={HaveAccount} />

# Header

This is a text that is dislayed if you already have an account (on sign up page) or don't have account yet (on a sign in page).

export const Component = (args) => (
  <HaveAccount>
    <p>{args.text}&nbsp;</p>
    <a>{args.link}</a>
  </HaveAccount>
);

<Canvas>
  <Story
    name="Have account"
    args={{
      text: "Already have an account?",
      link: "Sign In",
    }}>
    {Component.bind({})}
  </Story>
</Canvas>
