import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import SidebarItem from "./SidebarItem";
import { withNextRouter } from "storybook-addon-next-router";

<Meta title="Elements/Sidebar item" component={SidebarItem} decorators={[withNextRouter]} />

# Sidebar Item

This is a simple sidebar item. It has an icon from [feather icons pack](https://feathericons.com/) and a text.

export const Component = (args) => <SidebarItem {...args}></SidebarItem>;

export const ComponentSignOut = (args) => <SidebarItem {...args} type></SidebarItem>;

Component.story = {
parameters: {
nextRouter: {
path: '/dashboard',
asPath: '/dashboard',
},
},
};

<Canvas>
  <Story
    name="Default"
    args={{
      icon: "pie-chart",
      text: "Dasboard",
      url: "/dashboard",
    }}
    decorators={[withNextRouter]}>
    {Component.bind({})}
  </Story>
  <Story
    name="Sign out"
    args={{
      icon: "log-out",
      text: "Sign Out",
      url: "/dashboard",
    }}
    decorators={[withNextRouter]}>
    {ComponentSignOut.bind({})}
  </Story>
</Canvas>
