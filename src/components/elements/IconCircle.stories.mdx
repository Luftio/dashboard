import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import IconCircle from "./IconCircle";

<Meta title="Elements/Icon circle" component={IconCircle} />

# Icon circle

This is a icon circle used on events, suggestions and feedback pages.

export const Component = (args) => <IconCircle {...args} />;

<Canvas>
  <Story
    name="Icon circle"
    args={{
      iconName: "box",
      iconColor: "#fff",
      backgroundColor: "#F65656",
    }}>
    {Component.bind({})}
  </Story>
</Canvas>
