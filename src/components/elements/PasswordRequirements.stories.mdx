import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import PasswordRequirements from "./PasswordRequirements";

<Meta title="Elements/Password Requirements" component={PasswordRequirements} />

# Password Requirements

This is a simple text that is displayed under first input on the page where you can set your new password.

export const Component = (args) => (
  <PasswordRequirements>{args.text}</PasswordRequirements>
);

<Canvas>
  <Story
    name="Password Requirements"
    args={{
      text:
        "Password must be at least 12 characters long and contain a lowercase and an uppercase letter",
    }}
  >
    {Component.bind({})}
  </Story>
</Canvas>
