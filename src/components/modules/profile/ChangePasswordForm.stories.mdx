import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import ChangePasswordForm from "./ChangePasswordForm";

<Meta title="Modules/Forms" component={ChangePasswordForm} />

# Change password form

This is a form for updating password inside dashboard.

export const Component = () => <ChangePasswordForm />;

<Canvas>
  <Story name="Change password">{Component}</Story>
</Canvas>
