import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import ChangePasswordForm from "./ChangePasswordForm";

<Meta title="Modules/Change password form" component={ChangePasswordForm} />

# Change password form

This is a form for updating password inside dashboard.

export const Component = () => <ChangePasswordForm />;

<Canvas>
  <Story name="Change password form">{Component}</Story>
</Canvas>
