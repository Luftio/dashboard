import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import OnboardingFormResults from "./OnboardingFormResults";

<Meta title="Modules/Onboarding form results" component={OnboardingFormResults} />

# Onboarding form results

This is our onboarding form results. We collect from user data that we can than put into good use in future.

export const Component = () => <OnboardingFormResults />;

<Canvas>
  <Story name="Onboarding form results">{Component}</Story>
</Canvas>
