import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import SetNewPasswordForm from "./SetNewPasswordForm";

<Meta title="Modules/Set new password form" component={SetNewPasswordForm} />

# Set new password form

If user created an account and then wants to sign in and finds out that he forgets his password, he will then request a change. He would give us his email adress. We will then send an email to his inbox with a link. He then gets this form on a page, where he ca set his new password.

export const Component = () => <SetNewPasswordForm />;

<Canvas>
  <Story name="Set new password form">{Component}</Story>
</Canvas>
