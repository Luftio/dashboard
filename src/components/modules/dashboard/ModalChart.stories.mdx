import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import ModalChart from "./ModalChart";

<Meta title="Modules/Modal chart" component={ModalChart} />

# Modal Chart

More advanced chart for modal.

export const Component = (args) => <ModalChart {...args} />;

<Canvas>
  <Story
    name="Dashboard card"
    args={{
      data: {
        unit: "ppm",
        values: [
          { ts: new Date("2021-04-13T19:30:00Z"), value: 52 },
          { ts: new Date("2021-04-13T19:35:00Z"), value: 53 },
        ],
      },
    }}>
    {Component.bind({})}
  </Story>
</Canvas>
