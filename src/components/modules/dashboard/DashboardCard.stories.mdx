import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import DashboardCard from "./DashboardCard";

<Meta title="Modules/Dashboard" component={DashboardCard} />

# Dashboard card

This is main card on the dashboard page.

export const Component = (args) => <DashboardCard {...args} />;

<Canvas>
  <Story
    name="Card"
    args={{
      type: "score",
      score: 53,
      maxValue: 83,
      minValue: 30,
      change: 2,
      subheading: "Score",
    }}
  >
    {Component.bind({})}
  </Story>
</Canvas>
